# Định nghĩa biến môi trường (port 3000 giống file users.http)
@baseUrl = http://localhost:3000
@reportApi = {{baseUrl}}/reports

# Biến để lưu ID của report mới tạo
@reportId = 

### ===========================================
### 1. Tạo Báo cáo theo Ngày
### ===========================================
# Dùng @name để có thể lấy ID từ response
# @name createDailyReport
POST {{reportApi}}/daily
Content-Type: application/json

{
  "reportDate": "2025-11-13" 
}

# Tự động lưu ID của report vừa tạo

### ===========================================
### 2. Tạo Báo cáo theo Tuần
### ===========================================
POST {{reportApi}}/weekly
Content-Type: application/json

{
  "startDate": "2025-11-10",
  "endDate": "2025-11-16"
}

### ===========================================
### 3. Lấy danh sách tất cả báo cáo
### ===========================================
GET {{reportApi}}

### ===========================================
### 4. Lấy chi tiết một báo cáo theo ID
### ===========================================
# Tự động dùng ID từ report đã tạo ở bước 1
GET {{reportApi}}/{{reportId}}

# (Hoặc bạn có thể dán ID thủ công ở đây)
# GET {{reportApi}}/your_report_id_here

### ===========================================
### 5. Cập nhật báo cáo theo ID
### ===========================================
# Tự động dùng ID từ report đã tạo ở bước 1
PUT {{reportApi}}/{{reportId}}
Content-Type: application/json

{
  "notes": "Đây là ghi chú đã được cập nhật cho báo cáo."
}

### ===========================================
### 6. Xóa báo cáo theo ID
### ===========================================
# Tự động dùng ID từ report đã tạo ở bước 1
DELETE {{reportApi}}/{{reportId}}